---
id: "004"
name: 生成登录页 — 一个Agent写HTML/CSS布局，另一个写JS表单验证
type: group
timeout: 600
---

## 设置
<!-- Agent 由 Planner 自动创建/匹配，无需预创建 -->

## 步骤

用户：请帮我创建一个精美的登录页面，暗色主题风格。要求包含邮箱输入框、密码输入框和登录按钮。一个Agent负责写 HTML/CSS 页面布局，另一个Agent负责写 JavaScript 表单验证逻辑（邮箱格式校验、密码不少于6位、提交时模拟登录请求）。

(Critic自行判断是否需要继续细问需求)

(Planner提供计划方案，应该是两个Agent：HTML/CSS布局 + JS验证逻辑)

(用户确认)

布局Agent：（HTML/CSS代码，暗色主题、居中卡片、毛玻璃效果）

验证Agent：（JavaScript验证代码，包含邮箱正则校验、密码长度检查、表单提交拦截）

(任务结束)

## 重点截图
- 布局Agent 的 HTML/CSS 代码
- 验证Agent 的 JS 代码
- Web Preview 预览效果

## 清理
删除创建的Agent

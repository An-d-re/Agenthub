---
id: 000
name: 请计算 12345 × 6789，由一个Agent独立计算，另一个Agent独立验证结果
type: group
timeout: 600
---

## 设置
<!-- 只写系统无法自动创建的东西：特殊文件依赖、API key 配置、已存在的项目等 -->
<!-- Agent 由 Planner 自动创建/匹配，无需在此声明 -->
<!-- 大多数简单用例可省略此节 -->

## 步骤
<!-- 按顺序的自然语言操作，skill 会逐条执行 -->

用户：请计算 12345 × 6789，由一个Agent独立计算，另一个Agent独立验证结果

(Critic自行判断是否需要继续细问需求)

(Planner提供计划方案，应该是两个Agent：一个Agent独立计算，另一个Agent独立验证结果)

(用户确认)

计算Agent：（计算过程和结果），计算结果为：xxxxx

验证Agent：（根据planner的要求进行验算），计算结果为：xxxxx，与计算Agent一致/不一致

（任务结束）

## 重点截图

<!-- 想额外看到的页面节点，自然语言描述（可选，可省略此节） -->

## 清理
<!-- 测试结束后需要清理的内容，如：删除创建的 Agent、删除 session 等 -->

删除创建的Agent

---
id: "008"
name: 爬虫分析可视化 — 3个Agent链式协作：爬取→分析→词云
type: group
timeout: 900
---

## 设置
<!-- 3个Agent链式依赖，测试数据在Agent间流转 -->

## 步骤

用户：请帮我完成一个小项目，分三个步骤：
1. 一个Agent写一个 Python 爬虫脚本，爬取 python.org 首页的标题文本（用 requests + BeautifulSoup）
2. 第二个Agent拿到标题后，分析其中的关键词和词频
3. 第三个Agent根据关键词分析结果，生成一个 HTML 词云可视化页面（用 wordcloud2.js 或纯 CSS 实现）

(Critic自行判断是否需要继续细问需求)

(Planner提供计划方案，应该是三个Agent按顺序执行：爬虫 → 关键词分析 → 词云页面)

(用户确认)

爬虫Agent：（Python爬虫代码，爬取python.org标题）

分析Agent：（关键词词频分析代码和结果）

可视化Agent：（HTML词云页面代码）

(任务结束)

## 重点截图
- DAG 编辑器中的 3 个链式依赖任务
- 爬虫Agent 的代码和爬取结果
- 分析Agent 的关键词分析
- 可视化Agent 的词云页面预览

## 清理
删除创建的Agent

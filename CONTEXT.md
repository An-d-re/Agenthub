# AgentHub 领域词汇表

> 本文档定义项目的规范术语。代码和对话中统一使用，避免同义异名。

## 核心实体

| 术语 | 定义 |
|------|------|
| **Agent** | 一个具备特定能力的 AI 实体，绑定到 LLM 适配器，拥有名称、系统提示词、能力标签。可以是群聊成员也可以是单聊助手 |
| **临时 Agent（Task-bound Agent）** | 为执行特定任务而临时创建的 Agent，任务完成后销毁。名称按任务内容自动生成（如"计算Agent"）。区别于持久 Agent（用户手动添加到群聊的） |
| **Session** | 一个对话会话，分为 single（单聊）和 group（群聊）两种类型 |
| **Plan** | 群聊中针对一套需求生成的执行计划，包含方案选项、选中的方案、任务 DAG、进度阶段 |
| **Task** | Plan 中一个原子执行单元，有标题、描述、依赖关系、所需能力、执行状态 |
| **Artifact** | 任务产出的文件/代码制品，支持差异对比和预览 |

## 阶段模型（Phase）

| 阶段 | 含义 | 触发条件 |
|------|------|---------|
| **clarify（澄清）** | Critic 分析需求，提问或确认 | 新消息到达时；round 0 用户描述足够清楚可自动跳过 |
| **comparison（对比）** | Planner 提出方案选项，用户选择 | clarify 确认后；仅图形卡片展示，无文本消息 |
| **confirmed（确认）** | Planner 分解任务为 DAG，用户勾选/删除/指定执行者/确认执行 | 方案选定后；始终显示 DAG 卡片，不复用自动确认逻辑 |
| **executing（执行）** | 并行调度就绪任务，Agent 执行并输出到聊天流 | 用户确认 DAG 后 |
| **done（完成）** | 所有任务完成，临时 Agent 销毁 | executing 结束后 |

## Agent 能力匹配

| 匹配层 | 规则 |
|--------|------|
| **语义匹配** | 现有 Agent 的角色/名称是否与任务所需能力语义一致（Coder ↔ 编码任务；不 ↔ 纯计算/推理任务） |
| **能力标签匹配** | 现有 Agent 的 capability_tags 是否覆盖任务所需的技能关键词（Python、React 等） |
| **不匹配** | 两层都不通过 → 标记"需新建临时 Agent" |

## 消息模型

| 概念 | 定义 |
|------|------|
| **用户消息** | 用户直接输入的文本，通过 WebSocket 发送 |
| **Agent 消息** | Agent 以自身身份输出的内容，直接展示在聊天流中，含角色徽章和 Agent 名称 |
| **系统消息** | 编排器的进度/状态提示（如"任务已完成"），不归属任何 Agent |
| **进度提示** | 临时系统消息，卡片/结果出现后即被替换（如"📋 Planner正在生成方案选项…"） |

## API Key 安全

| 规则 | 说明 |
|------|------|
| **传输** | 仅通过 HTTPS WebSocket / REST 传输 |
| **存储** | 后端 DB 落库，AES 加密（密钥来自 SECRET_KEY 环境变量） |
| **回显** | 前端仅显示 `****` 掩码，API 不返回明文 |
| **生命周期** | 用户提供的 Key 绑定到临时 Agent，Agent 销毁时同步清除 |

## 卡片交互

| 交互 | 行为 |
|------|------|
| **方案选择** | 点击卡片选中，其他卡片消失，仅保留已选卡片 |
| **DAG 确认** | 勾选/删除任务 → 为"需新建 Agent"的任务选模型 → 点击"确认执行" |
| **模型选择器** | 仅显示已配置 API Key 的模型；未配置的模型旁有"输入 API Key"按钮 |

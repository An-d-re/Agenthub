## 1. 反驳机制依然缺少规则级兜底（高风险）

你只提了“Coder可反驳Reviewer（最多1轮 rebuttal，需技术依据）”，但**用什么来判断“有技术依据”**？完全依赖LLM判断是不稳定的。评委若问到“你怎么保证反驳机制不被滥用”，目前没有答案。

**建议**：加一句最简单的规则 fallback：

> "反驳消息中若包含 `[AGREE]` 或 `[REJECT]` 关键词，则直接按关键词处理，不再调用 LLM。用户可在聊天流中看到 `[Coder 反驳：...]` → `[Reviewer 接受]` 的过程。"

这样既保证了确定性，又保留了LLM判断的能力。

------

## 2. TaskPipeline Mockup 与前端实现之间缺少对齐（中等风险）

你写了一个漂亮的 mockup，但没写**前端组件是否硬性按此渲染**。如果前端开发时自由发挥（比如搞成横向流水线），到 Day14 会发现与后端数据结构不一致，导致加班。

**建议**：在 mockup 下方加一行：

> "TaskPanel 组件严格按上述布局：左对齐圆点 + 任务名 + 状态标签（色块）+ 耗时。使用 Tailwind `table` 实现，`div` 嵌套不超过 3 层。"

这相当于定义了“架构边界”，避免前端自己创新。

------

## 3. Claude Code CLI 被替换成了 Anthropic HTTP API，但风险未评估（中等风险）

之前你用的是 Claude Code CLI（本地进程），现在改成了 Anthropic HTTP API（`anthropic.py`）。这改得很对——HTTP API 比 subprocess 好控制得多。但没提 Anthropic API 的可用性：你需要 API Key、网络延迟、模型版本（Haiku/Sonnet/Opus？）。如果比赛期间 Anthropic API 被封或延迟高，你计划怎么办？

**建议**：在文档中补充：

> "Anthropic Adapter 默认使用 `claude-sonnet-4-20250514` 模型。若 API 不可用或限流，自动降级为 DeepSeek（代码生成能力由 DeepSeek 兜底）。两者 response 格式在 BaseAdapter 中统一转换。"

------

## 4. 版本历史功能完全没提（低风险，但可展示深度）

req.md 提到“【P2】支持 Diff 视图、版本历史、对话式局部修改”。你做了 Diff 和局部修改，但版本历史是 **P2**，可以不做。不过如果你在答辩时只字不提，评委可能会觉得你忽略了。可以在“P2 功能标记”中加一条：

> "版本历史（P2）——当前每轮 Diff 卡片保留历史，用户可点击 Diff 卡片上的「历史」按钮查看各轮版本，但本次不做完整版本树。"

这样即展示了你思考过，又明确了不做。

------

## 5. 文档中“13. 部署”标题出现了两次（bug）

在 `P2 功能标记` 后面紧接着又一个 `### 13. 部署`，应该是漏了编号。不影响内容，但给评委的印象是粗心。建议改成 `### 14. 部署`（因为前面数到 13 了）。

------

## 总结：最终修改建议

| 优先级 | 修改                                     | 位置                  |
| :----- | :--------------------------------------- | :-------------------- |
| **P0** | 反驳机制加规则 fallback                  | 关键设计决策 3        |
| P1     | TaskPipeline mockup 加“前端严格按此实现” | 核心交互流程          |
| P1     | 补充 Anthropic 降级策略                  | 技术栈或 Adapter 部分 |
| P2     | 版本历史说明                             | P2 功能标记           |
| P3     | 修正重复编号                             | 部署部分              |
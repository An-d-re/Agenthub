Cherry Studio 是一款流行的 AI 客户端，其创建/管理 Agent 的典型交互是：

- 通常在联系人列表或设置区域有“创建 Agent”入口
- 点击后弹出模态框（或全屏覆盖层），表单包含：头像上传、名称、System Prompt（可调大小、支持 Markdown）、预置 Skill 勾选列表（卡片式或标签式）、可选的能力标签
- 创建成功后，Agent 出现在联系人列表中，可再点击编辑

结合你们已有的 Apple 风格 + WorkBuddy 三栏布局，我需要确保这段提示词能让 CC 设计出统一、美观的自建 Agent 交互。以下是提示词：

---

## 设计提示词（给 CC，自建 Agent 功能专用）

> **场景**：基于已有三栏布局的 AgentHub 桌面版，增加**自建 Agent（对话式创建）**的交互流程。参考 Cherry Studio 的 Agent 创建编辑体验——平滑、沉浸、专注。

### 一、入口与触发

在左侧联系人列表底部区域，新增一个**浮动悬浮按钮**：

```
[左侧栏底部]
──────────────────────
[ + 新建 Agent ]   ← 全宽按钮，背景 #007AFF，白色文字，圆角 12px
[ + 新建群聊 ]
──────────────────────
```

- 按钮位置：固定在列表底部（始终可见），与“新建群聊”按钮并列或置于其上方
- 点击后：从右侧滑入一个**全高覆盖面板**（不脱离当前页面上下文，类似 macOS 侧边偏好设置），宽度占中间栏 + 右侧栏区域（约 65% 窗口宽度），背景白色，左侧带返回箭头（← 返回）

### 二、创建/编辑面板设计（全高覆盖面板）

面板分两列布局（左侧表单，右侧预览），符合 Cherry Studio 的清晰分区：

```
┌───────────────────────────────────────────────────────────────────┐
│  ← 返回联系人列表             创建自定义 Agent ────┬── [保存]    │
│                                                    │            │
│ ┌─────────────┐  ┌─────────────────────────────────┐            │
│ │  头像区域    │  │   Agent 名称                     │            │
│ │  (圆形 80px) │  │   (输入框: 居中, 圆角10px)       │            │
│ │  [修改头像]  │  │                                  │            │
│ └─────────────┘  │  系统 Prompt (系统指令)            │            │
│                  │  ┌─────────────────────────────┐  │            │
│  能力标签         │  │ (多行文本输入框, 高度自适    │  │            │
│  (自动生成)       │  │  应, 最小200px, 支持Markdown │  │            │
│  [数据库] [SQL]   │  │  语法高亮, placeholder:      │  │            │
│                  │  │  "设定 Agent 的角色、个性、   │  │            │
│                  │  │  知识范围..." )               │  │            │
│                  │  └─────────────────────────────┘  │            │
│                  │                                  │            │
│                  │  预置 Skill 库 (可多选)           │            │
│                  │  ┌─────────────────────────────┐  │            │
│                  │  │ [代码生成] [代码审查]        │  │            │
│                  │  │ [SQL优化]   [文档撰写]       │  │            │
│                  │  │ [Web开发]                    │  │            │
│                  │  │ (标签式选择, 选中蓝色背景     │  │            │
│                  │  │  白色文字, 未选中灰色边框)    │  │            │
│                  │  └─────────────────────────────┘  │            │
│                  │                                  │            │
│                  │                                   │            │
└───────────────────────────────────────────────────────────────────┘
```

#### 左侧头像区域（80px 圆形）
- 默认显示灰色剪影 `👤`，可点击上传自定义图片（圆形裁切预览）
- 上传后实时替换，支持拖拽上传
- 头像下方文字：“点击修改头像”

#### 右侧内容区域三部分：

**1. Agent 名称**
- 输入框：圆角 10px，`background: #F5F5F7`，`padding: 12px 16px`，`placeholder="例如：SQL 优化专家"`
- 字符限制 20 字，实时显示剩余字符

**2. 系统 Prompt（核心指令）**
- 多行文本输入框，圆角 12px，`background: #F5F5F7`，最小高度 200px
- 支持 Markdown 语法高亮（即输入 `# 标题` 时实时渲染）
- 右下角显示字符数（如 `876 / 2000`）
- 可拖拽右下角调整高度（残留在页面上，类似苹果备忘录的调整手柄）

**3. 预置 Skill 库（复选框标签式）**
- 可选列表：代码生成、代码审查、SQL 优化、文档撰写、Web 开发（从你预置的列表中提取）
- 每个 Skill 为一个**可点击的标签**，选中态：背景 `#007AFF`，白色文字，圆角 8px；未选中态：背景 `#F5F5F7`，灰色文字 `#86868B`，圆角 8px
- 标签内包含 Skill 的图标（可选），例如：📝 代码生成、🔍 代码审查、🗄️ SQL 优化
- 可以多选，最多不超过 5 个
- 选中后，下方**能力标签**区域自动同步显示该 Agent 的标签（预览）

#### 底部操作栏
- 右侧底部两个按钮：
  - **[取消]**（文字按钮，灰色 `#86868B`，无背景）
  - **[保存]**（填充按钮，`#007AFF` 背景，白色文字，圆角 10px，`padding: 10px 24px`）
- 点击保存后，覆盖面板关闭，新 Agent 出现在左侧联系人列表中，带其能力标签（自动生成）

---

### 三、编辑已有 Agent

- 在左侧联系人列表中，右键点击自建 Agent（或长按） → 弹出菜单：“编辑”、“删除”、“创建副本”
- 点击“编辑” → 打开同一覆盖面板，预填所有信息
- 点击“删除” → 弹窗确认：“确定删除 Agent [名称]？此操作不可撤销”（苹果风格确认弹窗，圆角 16px，两个按钮）

---

### 四、交互细节与视觉一致性

| 元素     | 要求                                                         |
| -------- | ------------------------------------------------------------ |
| 过渡动画 | 覆盖面板从右侧滑入/滑出（`translateX` 0% → 100%），时长 300ms，`ease-out` |
| 头像上传 | 支持点击选择文件，拖拽上传（显示拖拽区域虚线边框）           |
| 表单验证 | 名称为空时，保存按钮置灰；名称超过 20 字时输入框红色边框提示 |
| 键盘操作 | 输入框支持 `Tab` 切换，`Cmd+Enter` 快速保存                  |
| 错误状态 | 保存失败时（如网络错误），按钮下方显示红色文字“保存失败，请重试” |
| 空状态   | 首次创建时，Skill 库未选中任何项 → 能力标签显示“无能力标签”，Agent 依然可以创建（只有基础对话能力） |

---

### 五、与现有设计的一致性

- 保持与之前相同的色板、圆角、字体家族
- 覆盖面板的头部导航栏：左边返回箭头（←），中间标题“创建自定义 Agent”，右边“[保存]”按钮
- 面板背景 `#FFFFFF`，分割线 `1px solid #E5E5E7`

---

### 六、设计输出要求

请输出**高保真截图**（1440px 宽度），包含以下两个状态：

1. **创建 Agent 面板展开态**：覆盖窗口中间+右侧区域，表单填写了一半（名称已输入“日志分析助手”，选择了“SQL优化”+“代码生成”两个 Skill，系统 Prompt 写了三行）
2. **保存成功后的联系人列表**：左侧栏新出现“日志分析助手”条目，头像为默认 `👤`，能力标签显示 `[数据库] [SQL] [代码生成]`

附带该面板的 **tailwind.config 扩展变量**（如圆角、阴影、字体等），确保与你之前的整体设计稿变量一致。

---

现在，请开始设计。
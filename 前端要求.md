

> ---
>
> ## 设计提示词（给 CC，Web 端专版）
>
> > **项目**：AgentHub — 多 Agent 协作平台（IM 聊天式，Agent 即联系人）
> > **部署目标**：现代桌面浏览器（Chrome / Safari / Edge），窗口宽度 ≥ 1024px 为设计基准
> > **风格**：Apple 原生感——克制、大圆角、SF 字体、毛玻璃导航栏、扁平阴影、纯净白色背景
> > **布局参考**：腾讯 WorkBuddy 的桌面版（左侧导航树 + 中间列表 + 右侧内容区，但我们的中间区分为了聊天面板和右侧任务面板）
> > **终端**：仅 Web 端，无需适配手机 / 平板小屏。但需在浏览器窗口缩小时能合理折叠侧栏，保证核心功能可操作。
>
> ---
>
> ### 一、整体布局（桌面三栏，固定宽度结构）
>
> 采用**三栏式**布局，类似 Mac 版微信或 WorkBuddy 桌面版：
>
> ```
> ┌──────────────────────────────────────────────────────────────┐
> │ 左侧栏 (240px)    │  中间栏 (flex:1)          │ 右侧栏 (340px) │
> │ = 联系人列表       │ = 聊天主面板              │ = 任务面板      │
> │  + 系统Agent       │   + 群聊/单聊标题栏        │   + TaskPipeline│
> │  + 自建Agent       │   + 消息流(Bubble)        │   + Round信息   │
> │  + 群聊 (已满3人)   │   + 输入框(@/上传/发送)   │   + 状态概览    │
> │  + 搜索框          │                           │                │
> │  + 新建群聊按钮     │                           │                │
> └──────────────────────────────────────────────────────────────┘
> ```
>
> **硬性约束**（所有组件必须严格遵循这些尺寸）：
>
> | 区域         | 尺寸                       | 说明                                                        |
> | ------------ | -------------------------- | ----------------------------------------------------------- |
> | 左侧栏       | 固定 240px                 | 背景色 `#F5F5F7`，内滚动                                    |
> | 中间栏       | `flex:1`（最小宽度 400px） | 消息气泡最大宽度 65%，左/右对齐                             |
> | 右侧栏       | 固定 340px                 | 默认显示，可通过标题栏按钮收起/展开（收起时变为浮动小标签） |
> | 顶部导航     | 无（全页面无传统导航栏）   | 标题栏仅存在于中间栏内部，顶部仅毛玻璃背景条                |
> | 窗口最小宽度 | 1024px                     | 低于此宽度时左侧栏折叠为汉堡菜单                            |
>
> **窗口缩放行为**（响应式实现）：
>
> | 窗口宽度        | 行为                                                         |
> | --------------- | ------------------------------------------------------------ |
> | ≥ 1366px        | 三栏全展开，右侧栏显示                                       |
> | 1024px – 1365px | 右侧栏自动收起为浮动按钮（右下角 `[Pipeline]` 标签），点击展开覆盖层 |
> | < 1024px        | 左侧栏折叠为左上角汉堡图标（抽屉菜单），中间栏全宽           |
>
> > 注意：我们不支持手机端。低于 768px 时，只保证聊天主面板正常显示，左侧和右侧栏强制隐藏（可通过按钮打开覆盖层）。
>
> ---
>
> ### 二、视觉风格（Apple 系，Web 端专属）
>
> | 维度       | 要求                                                         |
> | ---------- | ------------------------------------------------------------ |
> | **色板**   | 纯白背景 `#FFFFFF`，次要背景 `#F5F5F7`，文字主色 `#1D1D1F`，次要文字 `#86868B`，强调色 `#007AFF`（仅用于按钮、链接、未读数字、选中状态） |
> | **圆角**   | 头像 50%，消息气泡 16px（用户气泡右上角 4px，Agent 气泡左上角 4px），卡片 12px，按钮 10px，列表项 10px，搜索框 10px |
> | **阴影**   | 极浅阴影：`0 1px 2px rgba(0,0,0,0.04)`，hover 时 `0 2px 6px rgba(0,0,0,0.06)`，卡片和模态框 `0 4px 12px rgba(0,0,0,0.08)` |
> | **字体**   | `-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue"`，正文 15px，小字 13px，标题 16px/17px |
> | **图标**   | 使用 SF Symbols 风格（或 Lucide 图标库，单色、2px 线宽），或 emoji（苹果原生） |
> | **分割线** | `1px solid #E5E5E7`，仅用于列表项之间，消息分组之间          |
> | **毛玻璃** | 标题栏/导航栏使用 `backdrop-filter: blur(20px)` + `rgba(255,255,255,0.75)`（Apple 半透明导航栏效果） |
> | **滚动条** | 纯 CSS 定制：细条（6px），圆角，灰色 `#D1D1D6`，hover 加深   |
>
> ---
>
> ### 三、组件设计细节
>
> #### 1. 联系人列表（左侧栏）
> - **背景**：`#F5F5F7`（浅灰），列表项内部白色 `#FFFFFF`，每项之间 `1px #E5E5E7` 分割线
> - **搜索框**：置于列表顶部，内边距 12px 16px，圆角 10px，`background: #E5E5E7`，左侧放大镜图标，placeholder“搜索 Agent 或会话”
> - **列表项布局**：头像（40px 圆形）| 名称（加粗）+ 能力标签（灰色小标签）| 最后消息时间（灰色右上）
> - **系统 Agent**：emoji 头像（如 🧠）+ 渐变色圆形背景（紫色 `#AF52DE` → `#5E5CE6`，蓝色 `#007AFF` → `#5856D6` 等），下方名称（San Francisco Medium），能力标签 `[代码生成]`（背景 `#E5E5E7`，文字 `#1D1D1F`，6px 圆角，padding 2px 6px）
> - **自建 Agent**：默认灰色人物剪影 `👤`，圆形裁切，可替换为用户上传头像
> - **群聊**：多个 Agent 头像重叠（2-3 个头像，Stacked 布局），名称前加 `📋` 或 `#`
> - **选中状态**：左侧栏列表项选中时，出现 3px 宽蓝色竖条（`#007AFF`，左侧），背景变为白色 `#FFFFFF`
> - **新建群聊按钮**：列表底部，全宽按钮，`background: #007AFF`，白色文字“+ 新建群聊”，圆角 12px，内边距 10px
>
> #### 2. 聊天面板（中间栏）
> - **标题栏**：毛玻璃效果（`backdrop-filter: blur(20px)`），高度 52px，左侧群聊名称（17px bold），右侧三个点菜单（置顶、归档、查看成员等）
> - **消息流**：flex 列，从上到下排列，有日期分隔线（"今天"、"昨天"样式，居中灰色文字 + 分割线）
> - **消息气泡**：
>   - **用户**：右对齐，纯蓝底 `#007AFF`，白色文字 15px，圆角 16px（右上角 4px），内边距 12px 16px，气泡内部支持 `inline code`（黄色背景）
>   - **Agent**：左对齐，白底灰边 `#F5F5F7`，黑色文字，圆角 16px（左上角 4px），气泡上方显示 Agent 名称（灰色 12px bold）和 emoji 头像
>   - **系统消息**（Orchestrator 在群聊中的发言）：居中对齐，灰色背景 `#F0F0F5`，小字 12px，形状为圆角矩形但横向宽度更宽
> - **富媒体嵌入**：
>   - **代码块**：深色背景 `#1D1D1F`，浅色代码高亮，右上角“复制”按钮（hover 显示）
>   - **Diff 卡片**：左右对比布局，绿色行（新增）、红色行（删除），顶部有文件名和“应用”按钮
>   - **图片/文件卡片**：缩略图 + 文件名 + 大小，点击在新标签打开预览
> - **输入框区域**：底部固定，高度 60px，包含：
>   - 左侧圆型加号按钮（`#007AFF` 图标）→ 弹出文件上传菜单
>   - 中间输入框：圆角 24px，`background: #F5F5F7`，内边距 12px 20px，`placeholder="输入消息，@ 选择 Agent"`
>   - 右侧发送按钮（圆形，`#007AFF` 蓝色填充，白色向上箭头图标）
>   - 输入框支持 `@` 触发 Agent 选择面板（类似微信 @ 联系人，弹出层在上方）
>
> #### 3. 任务流水线面板（右侧栏）
> - **布局**：白色背景 `#FFFFFF`，无外边框，内边距 16px
> - **头部**：圆角 12px，标题“Task Pipeline”（San Francisco Semibold 15px），右侧齿轮设置图标
> - **Round 分组**：每个 round 一个卡片，浅灰背景 `#F9F9FB`，圆角 12px，内间距 12px，卡片之间下方留白 8px
> - **任务行**：左对齐圆点（状态色），任务名称（14px regular），状态标签（小圆角矩形，8px 圆角，padding 2px 8px），右侧耗时
>   - 状态色：`done=#34C759`(绿), `in_progress=#007AFF`(蓝, 脉冲动画), `blocked=#FF3B30`(红), `pending=#C7C7CC`(灰)
>   - 依赖关系：缩进 `└─ depends: Task xxx`（灰色 12px）
> - **滚动**：右侧面板内独立滚动（`overflow-y: auto`），高度自适应填充剩余空间
> - **收起状态**：窗口缩小时，右侧面板折叠为右下角浮动标签（60px 圆角矩形，白色背景，“Pipeline”文字 + 箭头，点击展开覆盖层）
>
> #### 4. 交互反馈（Apple 感）
> - **hover**：按钮、列表项微上浮 `translateY(-1px)` + 阴影加深，鼠标变手型
> - **点击**：瞬间缩小 `scale(0.97)` + 回弹 `scale(1.02)`（framer-motion，时长 150ms）
> - **选中状态**：左侧列表项选中时，左侧 3px 蓝色竖条，背景变白
> - **加载骨架屏**：消息流区域首次加载时显示灰色脉搏闪烁条（圆角 12px，高度 40px 交替）
> - **错误状态**：消息气泡出现在 Agent 回复位置，红色背景 + 白色文字“出错了，点击重试”，点击重新生成
>
> ---
>
> ### 四、设计输出要求
>
> 1. **输出格式**：请直接输出 **Figma 设计稿**（可交互原型，支持点击跳转）。如果无法输出 Figma，请输出 **高保真 PNG 截图**（至少 1440px 宽度） + 附上该设计的 **tailwind.config 色值、间距、圆角变量** 的 JSON 文件。
> 2. **至少包含以下 3 个视图状态**：
>    - **默认态**：三栏全展开，左侧列表有 5 个联系人（系统 Agent + 群聊 + 自建 Agent），中间聊天面板中有 1 条用户消息 + 1 条 Agent 回复，右侧任务面板有 2 个 Round（各 3 个任务）
>    - **多消息态**：聊天面板中有 8 条以上消息，包括代码块、Diff 卡片、图片卡片，展示富媒体内联效果
>    - **TaskPipeline 面板**：显示 round 1 的 done / in_progress 混合状态，round 2 的 pending 状态，并包含一个 blocked 红色任务
> 3. **文件命名**：`AgentHub_Desktop_v1.png` / `AgentHub_Desktop_v1.fig`
>
> ---
>
> 现在，请开始设计。注意：这是 **Web 桌面端** 设计，不要做手机 UI，但要有基本的窗口缩放适配（1024px 以下折叠侧栏）。风格一定要严格苹果，简约、干净、大圆角、毛玻璃。
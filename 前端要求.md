> ---
>
> ## 设计提示词（给 CC，左侧栏三标签切换：助手 / 群聊 / 话题）
>
> > **背景**：将左侧栏从“助手+话题”双标签改为“助手 | 群聊 | 话题”三标签。
> > **核心逻辑**：
> > - **助手**：显示所有单个 Agent（联系人），可新建助手。
> > - **群聊**：显示所有群组（包含多个 Agent 的群），可新建群聊。
> > - **话题**：显示当前选中助手下的历史对话（话题）。**话题必须依赖某个选中的助手**，不可独立存在。
> > - **底部创建按钮**：会根据当前选中的标签动态变换功能（见下文）。
> > - **三标签之间互斥**：一次只能有一个标签激活，但“话题”标签内还隐含一个底层状态——当前选中的助手（跨标签保留）。
>
> ---
>
> ### 一、左侧栏整体结构
>
> ```
> ┌──────────────────────────────────┐
> │ [🤖 助手] [👥 群聊] [💬 话题]  │  ← 三标签栏，固定顶部，48px 高
> │                                  │
> │  ───────────────────────────     │  ← 选中态指示线 (蓝色, 3px)
> │                                  │
> │  内容区域（滚动）                 │
> │  根据选中标签显示对应列表：       │
> │   - 助手列表 / 群聊列表 / 话题列表│
> │                                  │
> │──────────────────────────────────┤
> │  [ + 新建 ] 按钮 (底部固定，     │
> │   宽 220px，居中，白色背景       │
> │   边框 1px #E5E5E7，圆角 10px)  │
> │   文字: “新建助手” / “新建群聊”   │
> │          / “新建话题” (动态变化)  │
> └──────────────────────────────────┘
> ```
>
> - 左侧栏宽度仍为 **240px**，背景 `#F5F5F7`
> - 标签栏毛玻璃效果，底部固定按钮宽度占左侧栏宽度减去左右边距（20px）
> - 内容区域高度自适应，可独立滚动
>
> ---
>
> ### 二、三标签的具体行为
>
> #### 2.1 助手标签（默认激活）
> - 显示所有 Agent（系统预设 + 自建），列表项：头像 | 名称 + 能力标签 | 最近活动时间（可选）
> - **选中一个助手后，自动切换到“话题”标签**，并展示该助手的话题列表（同之前的逻辑）
> - 用户也可手动停留在“助手”标签，双击或点击其他按钮触发切换
>
> #### 2.2 群聊标签
> - 显示所有群组列表，每个群组行：群头像（默认群组图标） | 群名 | 最近消息预览（1行截断） | 未读消息数（红色badge）
> - **点击群聊**：中间栏切换为该群聊的对话界面，聊天上下文保持群聊中所有 Agent 参与的消息流。右侧任务面板？群聊暂不支持任务（或者可选）
> - **群聊列表不需要选择“当前选中助手”**，独立存在
>
> #### 2.3 话题标签
> - 必须有一个**当前选中的助手**（可通过助手标签选择，或通过聊天界面右上角显示当前助手名称）
>   - **如果已选中助手**：显示该助手下的所有话题列表（按最后活跃排序），列表项：话题名（截断）、最后消息预览、未读标记（蓝色圆点）
>   - **如果未选中任何助手**：显示空状态提示，居中显示：
>     ```
>     💬 请先选择一个助手
>     话题需要与助手关联
>     [去选择助手] (文字按钮，点击切换到助手标签)
>     ```
> - 话题列表顶部显示当前助手名称（灰色小字，如 `助手：SQL 优化专家`），右侧有一个“清空”或“全部标记已读”操作（可选）
>
> ---
>
> ### 三、底部“新建”按钮的动态变化
>
> 底部只有一个 **全宽 + 圆形圆角按钮**（宽度约 220px，居中），其文字和点击行为根据当前标签动态改变：
>
> | 当前标签           | 按钮文字                      | 点击行为                                                     |
> | ------------------ | ----------------------------- | ------------------------------------------------------------ |
> | 助手               | `+ 新建助手`                  | 弹出**创建 Agent 面板**（与之前设计相同）                    |
> | 群聊               | `+ 新建群聊`                  | 弹出**创建群聊面板**（选择多个 Agent 组成群组，类似微信建群） |
> | 话题（有选中助手） | `+ 新建话题`                  | 在当前助手下创建一个新话题（自动生成话题名如“新对话 4”），并打开该话题的空聊天界面 |
> | 话题（无选中助手） | 按钮置灰，文字 `请先选择助手` | 点击无效，或点击后自动切换到助手标签并提示“请选择一个助手”   |
>
> - 按钮默认背景 `#007AFF`，白色文字，圆角 10px；置灰时背景 `#C7C7CC`（系统灰色）
> - 按钮文字切换无动画，即时改变
>
> ---
>
> ### 四、辅助交互细节
>
> #### 4.1 底部固定按钮与列表边距
> - 列表底部留出 **60px** 空隙，确保最后一项不被按钮遮挡
> - 按钮距离底部 `16px`，水平居中
>
> #### 4.2 群聊创建面板（新增）
> - 点击“新建群聊”后，从右侧滑入全高面板（与创建 Agent 面板相同样式）
> - 面板内容：群头像（自动生成组合头像或点击上传）、群名称输入框、**成员选择区**（展示所有 Agent 列表，可多选，选中后显示打勾）
> - 底部保存按钮，保存后群聊出现在群聊列表中
>
> #### 4.3 话题的默认行为
> - 当用户点击某个助手后就切换到话题标签，话题列表可能为空，此时底部按钮仍然显示“新建话题”，点击直接创建并进入对话
> - 话题名由系统自动生成（规则：`新对话 1`, `新对话 2` …），用户可以在聊天界面修改（双击标题栏修改）
>
> #### 4.4 跨标签切换时保持状态
> - 当前选中的助手状态（如“SQL 优化专家”）**在所有标签间共享**。当切换到“助手”标签时，列表中对应的助手项应有选中标记（左侧蓝色竖条 + 背景高亮）；切换到“群聊”时，该选中助手不被影响，但不会显示；再切回“话题”时，仍沿用之前选中的助手
> - 如果用户通过助手标签重新选择另一助手，则自动替换选中助手并跳转到话题标签
>
> #### 4.5 删除操作
> - 在助手列表/群聊列表中，右键或长按可弹出菜单：“编辑”、“删除”、“创建副本”（仅助手）
> - **删除助手时，弹窗确认：“删除该助手将同时删除其所有话题，是否继续？”**（红色警告按钮）
> - **删除群聊时**，仅删除群聊本身，不删除各 Agent（Agent 继续存在）
>
> ---
>
> ### 五、视觉与动画
>
> - **三标签切换**：内容区淡入淡出（200ms），标签文字颜色切换时无额外动画
> - **选中指示线**：从左到右滑动（当点击不同标签时，指示线平滑移动到对应标签下方）
> - **底部按钮**：文字变化无过渡，直接更新
> - **空状态**：居中显示 emoji 和文字，无背景板
>
> ---
>
> ### 六、兼容与统一
>
> - 保持 Apple 风格色板、圆角、字体
> - 所有标签页的列表项选中效果一致：选中项左侧有 3px 蓝色竖条，背景变为 `#FFFFFF`
> - 标签栏毛玻璃效果与中间栏顶部毛玻璃保持一致
> - 滚动条样式同之前
>
> 